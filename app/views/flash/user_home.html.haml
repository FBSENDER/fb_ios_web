- title("手游加速器")

.container
  .row
    .col-sm-12.col-xs-12.col-md-12
      %h4.page-header.text-center{style: "margin-bottom: 10px"} 手游加速器
      %p.text-right
        %small
          韩服、美服手游加速，新用户送
          %span{style: "color:red"}100M
          流量
      %p.text-right
        %small
          %span{style: "color:red"}一键加速
          ，老司机开车带你飞
      %p.content.alert.alert-info{style:"padding-left:20px"} 
        剩余流量：#{traffic_desc(@user.transfer_enable - @user.d)}
        %small
          （下拉刷新）
      - if @is_new
        %p.content.alert.alert-warning{style:"padding-left:20px"} 
          %small
            欢迎新司机，加速服务正在初始化，大约需要5分钟
      - if @user.check_in_times > 0
        %p{style:"padding-left:20px"} 
          %small 签到次数：
          #{@user.check_in_times}次
      - if @user.gift_transfer
        %p{style:"padding-left:20px"}
          %small 签到赚取总流量：
          #{traffic_desc(@user.gift_transfer)}
      - if @user.last_check_in_time > 0
        %p{style:"padding-left:20px"}
          %small 上次签到时间：
          #{Time.at(@user.last_check_in_time).strftime('%Y-%m-%d  %H:%M')}
      - if @user.last_gift_transfer
        %p{style:"padding-left:20px"} 
          %small 上次签到获得流量：
          #{traffic_desc(@user.last_gift_transfer)}
      - if @user.check_in_times > 0
        %p{style:"padding-left:20px"} 
          %small 签到次数：
          #{@user.check_in_times}次
      %p{style:"padding-left:20px"} 
        %small 当前活动：
        每日签到送流量 5MB - 10MB
      %p{style:"padding-left:20px"} 
        %small 时差派游戏加速器

      %p.content.text-right{style: "padding-top:0"}
        - if @checked
          %a.btn.btn-warning 今日已签
        - else
          %a.btn.btn-success{href: "/flash/#{params[:uuid]}/check_in/?isFirst=0"} 立即签到
      - if @checked
        %p.content.text-right{style: "padding-top:0"}
          %a.btn.btn-primary{href: "https://www.google.com", target: "_blank"} Google
          %a.btn.btn-primary{href: "http://www.youtube.com", target: "_blank"} YouTube 
          %a.btn.btn-primary{href: "http://www.twitter.com", target: "_blank"} Twitter
        %p.content.text-right{style: "padding-top:0"}
          %small{style: "padding-top:20px"} 访问以上网站测试加速器连通情况
      
      %div{style: "height: 50px"}
